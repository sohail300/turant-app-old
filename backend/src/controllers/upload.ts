export const isBlocked = (req, res) => {
  return res.json({ msg: "Is Blocked" });
};

export const uploadArticle = (req, res) => {
  return res.json({ msg: "Article" });
};

export const uploadImage = (req, res) => {
  return res.json({ msg: "Image" });
};

export const uploadVideo = (req, res) => {
  return res.json({ msg: "Video" });
};
