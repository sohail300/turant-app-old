# upstream frontend {
#     server frontend:3000;
# }

upstream apiserver {
    server backend:3000;
}

server {
    listen 80;

    # location / {
    #     proxy_pass http://frontend;
    # }

    location /api {
        proxy_pass http://apiserver;
    }
}